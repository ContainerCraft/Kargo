config:
  kargo:kubernetes:
    context: usrbinkat-optiplexprime
  kargo:talos:
    controlplane: ha
    enabled: true
    image: docker.io/containercraft/omni
    running: true
    workers: 1
  kargo:vm:
    enabled: false
